# dsh-worklog — bundle patch layer.
#
# 宿主 row 放在 profile 的 cordis.patch.yml（insert）里，此处不再插入 row 以
# 避免重复。本包进入 dsh.profile.bundles 列表，使 clientModules 在 web 启动时
# 扫描到 dsh.client 元数据并注册「工作台」client 模块（lib/client.js）。
[]
